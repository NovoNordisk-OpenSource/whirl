---
title: "`r params$title`"
date: "`r Sys.time()`"
date-format: "YYYY-MM-DDTHH:mm:ss zzz"
params:
  title: 'dev'
  script_md: 'dev'
  strace: FALSE
  strace_path: ''
  renv: TRUE
format:
  html:
    embed-resources: true
    toc: true
    toc-depth: 3
    smooth-scroll: true
---

# Summary

```{r, echo=FALSE}
if (is.character(params$renv) && params$renv == "yes" || is.logical(params$renv) && params$renv){
  whirl:::renv_status()
}
```

```{r, echo=FALSE}
if (is.character(params$strace) && params$strace == "yes" || is.logical(params$strace) && params$strace){
  whirl:::readstrace_info(params$strace_path)
}
```

# Script

```{r, child = params$script_md}
```
