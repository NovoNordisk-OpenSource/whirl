# Read strace

```{r}

paste0(root,".strace") |> 
  NNlog:::read_strace() |> 
  tibble::as_tibble() |> 
  dplyr::select(file, action) |> 
  dplyr::filter(!grepl("^/scer/localprojstat|^~/.cache", file)) |> #Only to remove clutter for now
  dplyr::distinct() |> 
  print(n = 200)
```